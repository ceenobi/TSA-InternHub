- 2026-08-28 — lint: ESLint was a dead gate — the repo had no `eslint.config.*` and the CI `lint` job used `continue-on-error: true`, so it always passed. Added a flat `eslint.config.js` (typescript-eslint recommended + `react-hooks/rules-of-hooks`). `no-explicit-any` / `no-unused-vars` are warnings for now (pervasive legacy usage) — tighten to errors after cleanup. Do NOT set `continue-on-error` on the lint job again.
- 2026-08-28 — react-router: `app/components/ui/logo.tsx` `size` prop is a **string** (tailwind class, e.g. `"size-9"`), not a number. Passing `size={24}` fails typecheck; callers must pass a string class.
- 2026-08-28 — vercel: Vercel's build runs classic `yarn 1.22.19`, which cannot parse Yarn-Berry `resolutions` keys, so `yarn install` fails. Fix by setting the Vercel project Install Command to `corepack yarn@4.16.0 install`. Also, the Hobby plan allows only one cron per day — keep `vercel.json` crons at `0 0 * * *`.
- 2026-08-28 — CI security: GitHub Actions workflows must declare an explicit `permissions:` block (least-privilege) or CodeQL flags `actions/missing-workflow-permissions`. For jobs that only read the repo, set `permissions: { contents: read }` at the workflow level.
