# Agent Instructions

## Git workflow

- Push changes to the `testing` branch (not `main`).
- Create a PR from `testing` to `main`.
- Wait for all CI checks to pass.
- Once checks pass, merge the PR to `main`.
- Sync local `main` after merge.
